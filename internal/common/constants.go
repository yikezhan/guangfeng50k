package common

// CookieName cookie key
//数据是否有效
const (
	Valid    = int32(0)
	NotValid = int32(1)
)
const (
	PlayerNumber = 4
)
const (
	No   = 0
	Win  = 1
	Fail = 2
)
const (
	Draft   = 0 //初始化状态
	Cal     = 1 //结算中
	Confirm = 2 //已确认
)
